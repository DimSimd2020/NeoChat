# NeoChat: Связь без серверов и без интернета

## Проблема (честно)

Если интернет заблокирован и нет VPS — у тебя нет сети.
Нельзя передать данные по каналу, которого не существует.

Но есть каналы, которые НЕ контролируются интернет-блокировкой:

| Канал              | Дальность    | Скорость   | Стоимость  |
|--------------------|-------------|------------|------------|
| Bluetooth 5.0      | 10-100м     | 2 Мбит/с   | Бесплатно  |
| WiFi Direct        | 50-200м     | 250 Мбит/с | Бесплатно  |
| NFC                | 5 см        | 424 Кбит/с | Бесплатно  |
| SMS                | Безлимит    | ~1 КБ/с    | По тарифу  |
| Звуковой канал     | 1-5м        | ~100 бит/с | Бесплатно  |

**Ключевая идея:** Если каждое устройство с NeoChat — это узел сети,
то сеть строится из самих пользователей. Чем больше людей поставили
NeoChat — тем плотнее сеть, тем надёжнее связь.

---

## Архитектура: P2P Mesh Network

```
Сценарий: Ты в Москве, друг в Питере, интернет заблокирован.

Ты (Москва)
  ↓ Bluetooth
Прохожий с NeoChat (метро)
  ↓ Bluetooth
Студент с NeoChat (вокзал)
  ↓ WiFi Direct
Пассажир Сапсана с NeoChat
  ↓ ... (цепочка)
Человек с NeoChat (Питер)
  ↓ Bluetooth
Твой друг (Питер)
```

Это называется **store-and-forward mesh** — сообщение хранится на
промежуточных устройствах и передаётся дальше при встрече с другими
узлами сети. Как почтовые голуби, только через Bluetooth.

**Прецеденты:**
- **Briar** — мессенджер, работает через Bluetooth/WiFi mesh
- **Bridgefy** — использовался на протестах в Гонконге (2019)
- **FireChat** — mesh-чат, миллионы пользователей
- **Meshtastic** — mesh через LoRa-радиомодули

---

## Три режима NeoChat

### Режим 1: Интернет (нормальный)
```
[A] ←——— P2P через QUIC ———→ [B]
```
- Прямое шифрованное соединение
- Мгновенная доставка
- Иконка: 🌐

### Режим 2: Mesh (интернета нет)
```
[A] →BT→ [X] →BT→ [Y] →WiFi→ [Z] →BT→ [B]
```
- Сообщение шифруется E2E (только A и B могут прочитать)
- Промежуточные узлы НЕ видят содержимое
- Доставка: от минут до часов/дней
- Работает без интернета вообще
- Иконка: 🕸️

### Режим 3: SMS (экстренный)
```
[A] →SMS→ [Оператор] →SMS→ [B]
```
- Шифрованный текст через обычные СМС
- Мгновенная доставка
- Платно по тарифу оператора
- Иконка: 📱

---

## Как работает Mesh — детали

### Обнаружение соседей

Телефон с NeoChat постоянно (раз в 30 сек) шлёт BLE beacon:

```
BLE Advertisement:
  Service UUID: 0xNEO1
  Data: [protocol_version][user_id_hash_4bytes]
```

- Не содержит имени или данных пользователя
- Только короткий хеш для идентификации "я узел NeoChat"
- Батарею почти не жрёт (BLE Advertising = микроватты)

Когда два устройства NeoChat оказываются рядом:

```
1. BLE scan обнаружил соседа
2. Подключение по Bluetooth/WiFi Direct
3. Обмен "почтой":
   - "У меня есть сообщения для user_hash_XXXX, YYYY, ZZZZ"
   - "Мне нужны сообщения для user_hash_AAAA"
   - Передача шифрованных пакетов
4. Отключение
```

### Протокол передачи

```rust
/// Пакет для mesh-передачи
pub struct MeshPacket {
    /// ID сообщения (для дедупликации)
    pub message_id: String,

    /// Хеш ID получателя (не полный ID — приватность)
    pub recipient_hash: [u8; 4],

    /// Зашифрованный payload (только получатель расшифрует)
    pub encrypted_payload: Vec<u8>,

    /// TTL — сколько хопов осталось (макс 20)
    pub ttl: u8,

    /// Timestamp создания (для очистки старых)
    pub created_at: u64,

    /// Размер в байтах
    pub size: u32,
}
```

### Правила маршрутизации

1. **Каждый узел хранит чужие пакеты** до 7 дней или 50 МБ
2. **При встрече с новым узлом** — обмен пакетами, которых у другого нет
3. **TTL уменьшается** при каждом хопе (защита от бесконечных петель)
4. **Дедупликация** по message_id (не передаём то, что уже есть)
5. **Приоритет** — свежие сообщения передаются первыми

### Приватность промежуточных узлов

```
Промежуточный узел знает:         Промежуточный узел НЕ знает:
✗ 4 байта хеша получателя         ✗ Кто отправитель
✗ Размер сообщения                ✗ Кто получатель (только хеш)
✗ Время создания                  ✗ Содержимое сообщения
                                  ✗ Тему/тип сообщения
```

---

## Масштабирование: эффект сети

### Плотность покрытия

| Пользователей в городе | Среднее время доставки |
|------------------------|----------------------|
| 100                    | 1-3 дня              |
| 1,000                  | 2-12 часов           |
| 10,000                 | 10-60 минут          |
| 100,000                | 1-10 минут           |
| 1,000,000              | < 1 минуты           |

В метро, торговых центрах, вокзалах — плотность выше,
доставка быстрее. Посчастливится встретить человека с интернетом —
сообщение уйдёт через интернет мгновенно.

### Гибридный режим

Если у ОДНОГО узла в цепочке есть интернет — он становится мостом:

```
[A] →BT→ [X] →BT→ [Y (есть интернет!)] →QUIC→ [B]
                    ↑
                    Мост: автоматически отправляет
                    накопленные пакеты через интернет
```

Это критически важно: даже при массовой блокировке всегда
найдётся кто-то с VPN/спутником/роумингом.

---

## Технологии для реализации

### Android
```kotlin
// BLE Advertising — постоянно объявляем себя
val advertiser = BluetoothLeAdvertiser()
advertiser.startAdvertising(settings, advertiseData, callback)

// BLE Scan — ищем соседей
val scanner = BluetoothLeScanner()
scanner.startScan(filters, scanSettings, scanCallback)

// WiFi Aware (Android 8+) — обнаружение без AP
val wifiAwareManager = getSystemService(WifiAwareManager::class.java)

// Nearby Connections API (Google) — упрощённый mesh
Nearby.getConnectionsClient(context)
    .startAdvertising("NeoChat", SERVICE_ID, connectionCallback, options)
```

### Desktop (Windows/Linux/Mac)
```rust
// Bluetooth через btleplug (кросс-платформенный)
use btleplug::api::{Central, Peripheral};

// mDNS для обнаружения в локальной сети
use mdns_sd::{ServiceDaemon, ServiceInfo};

// WiFi Direct через platform-specific API
// Windows: WiFiDirectDevice API
// Linux: wpa_supplicant P2P
```

### iOS
```swift
// Multipeer Connectivity Framework — встроенный mesh от Apple
let session = MCSession(peer: myPeerID)
let advertiser = MCNearbyServiceAdvertiser(peer: myPeerID,
    discoveryInfo: nil, serviceType: "neochat")
let browser = MCNearbyServiceBrowser(peer: myPeerID,
    serviceType: "neochat")
```

---

## Стоимость инфраструктуры

```
Серверы:           $0 (нет серверов)
CDN:               $0 (не нужно)
База данных:       $0 (каждое устройство — своя БД)
Пропускная:        $0 (Bluetooth/WiFi бесплатны)
Домен:             $0 (не нужен)
Сертификаты:       $0 (свои ключи X25519)
───────────────────────────────────
Итого:             $0/мес при любом количестве пользователей
```

---

## Roadmap реализации

### Этап 1: Локальный mesh (ближайший)
- [ ] BLE advertising/scanning на Android
- [ ] Nearby Connections API интеграция
- [ ] Store-and-forward хранилище пакетов
- [ ] Передача шифрованных пакетов при обнаружении соседа
- [ ] Desktop: Bluetooth через btleplug

### Этап 2: WiFi Direct
- [ ] WiFi Aware на Android 8+
- [ ] Быстрая передача больших объёмов (фото, файлы)
- [ ] Автоматическое переключение BLE → WiFi Direct при близости

### Этап 3: Гибридный мост
- [ ] Автоопределение наличия интернета
- [ ] Мост: mesh-пакеты → интернет → получатель
- [ ] Обратный мост: интернет → mesh для оффлайн-получателей

### Этап 4: Оптимизация
- [ ] Geo-routing (приоритет пакетов в направлении получателя)
- [ ] Epidemic vs Spray-and-Wait (экономия батареи)
- [ ] Репутация узлов (защита от спама в mesh)
